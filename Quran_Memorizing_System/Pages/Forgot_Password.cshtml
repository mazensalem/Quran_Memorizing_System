@page
@model Quran_Memorizing_System.Pages.Forgot_PasswordModel
@{
    ViewData["Title"] = "Forgot Password";
}

@section Styles {
    <style>
        .forgot-wrapper {
            max-width: 480px;
            margin: 80px auto;
        }

        .section-title {
            font-size: 24px;
            font-weight: 600;
            text-align: center;
        }

        .section-divider {
            width: 60px;
            height: 3px;
            background: #333;
            border-radius: 3px;
            margin: 10px auto 25px;
        }

        .admin-box {
            background: #fff;
            padding: 25px 30px;
            border-radius: 10px;
            border: 1px solid #e1e1e1;
            box-shadow: 0 3px 8px rgba(0,0,0,0.05);
        }

        .admin-label {
            font-weight: 600;
        }

        .form-control {
            border-radius: 6px;
        }

        .btn-main {
            border-radius: 6px;
            background: #222;
            color: white;
        }

            .btn-main:hover {
                background: #000;
            }
    </style>
}

<div class="row justify-content-center">
    <div class="col-lg-6">
        <div class="forgot-wrapper">
            <section class="mb-4">
                <div class="section-title">Forgot Password</div>
                <div class="section-divider"></div>

                <div class="admin-box">
                    <p class="text-muted">Enter your email and we'll send you a reset code.</p>

                    <form method="post">
                        <div class="mb-3">
                            <label class="admin-label" for="email">Email Address</label>
                            <input type="email" id="email" asp-for="@Model.Email" name="email" class="form-control" placeholder="Enter your email" required />
                            <span class="invalid-feedback d-block" asp-validation-for="@Model.Email"></span>
                        </div>

                        <div class="mb-3">
                            <label class="admin-label" for="role">Role</label>
                            <select id="role" class="form-select" asp-for="@Model.role">
                                <option selected disabled>Select role</option>
                                <option>Participant</option>
                                <option>Sheikh</option>
                            </select>
                            <br />
                            <span class="invalid-feedback d-block" asp-validation-for="@Model.role"></span>
                        </div>

                        <button type="submit" class="btn btn-main w-100">Send Reset Code</button>
                    </form>

                    @if (TempData["Info"] != null)
                    {
                        <div class="alert alert-success mt-3">
                            ✅ Reset code sent to your email.
                        </div>
                    }

                    @if (TempData["Error"] != null)
                    {
                        <div class="alert alert-danger mt-3">
                            @TempData["Error"]
                        </div>
                    }


                </div>
            </section>
        </div>
    </div>
</div>