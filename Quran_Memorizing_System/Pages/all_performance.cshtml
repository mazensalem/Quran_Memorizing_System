@page
@model Quran_Memorizing_System.Pages.all_performanceModel
@{
	@using System.Data
}

@for (int i=0;i<Model.submission.Rows.Count;i++)
{
	<p>Rating: @Model.submission.Rows[i]["Rating"]</p>
	<p>Exam titel: @Model.Examtitles[i]</p>
	<p>Sheikh Email: @Model.submission.Rows[i]["Sh_Email"]</p>
}